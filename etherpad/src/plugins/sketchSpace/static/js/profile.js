dependencies = {
  layers: [
    {
      name: "sketchspace.js",
      discard: false,
      dependencies: [
        "sketchSpaceDesigner.main"
      ]
    }
  ],
  prefixes: [
	     ["dojo", "dojo"],
	     ["dijit", "dijit"],
	     ["dojox", "dojox"],
	     ["sketchSpaceDesigner", "sketchSpaceDesigner"]
  ]
};
